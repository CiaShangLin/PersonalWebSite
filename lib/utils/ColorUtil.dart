
import 'dart:ui';

import 'package:flutter/material.dart';

// menu顏色
const menu_color = Color.fromARGB(255, 189, 93, 56);

// 頭像邊框
const avatar_color = Color.fromARGB(255, 151, 74, 45);

// menu文字選中顏色
const tab_selected_color = Colors.white;

// menu文字沒選中顏色
const tab_unselected_color = Color.fromARGB(100, 33, 37, 41);

//技能分割線顏色
const resume_divider_color = Color.fromARGB(100,255,205,77);

//技能元點顏色
const skill_dot_color = Color.fromARGB(100, 85, 85, 85);

//技能文字文字顏色
const skill_text_color = Color.fromARGB(255, 0, 0, 0);